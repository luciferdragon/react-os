import React from 'react';

var ROSButton = React.createClass({
    render() {
        return (
            <div className="ros-button">
            </div>
        );
    }
});

export default ROSButton;
