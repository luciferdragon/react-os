import React from 'react';

var ROSContextMenu = React.createClass({
    render() {
        return (
            <div className="ros-contextmenu">
            </div>
        );
    }
});

export default ROSContextMenu;
