import React from 'react';

var ROSWindow = React.createClass({
    render() {
        return (
            <div className="ros-window">
            </div>
        );
    }
});

export default ROSWindow;
